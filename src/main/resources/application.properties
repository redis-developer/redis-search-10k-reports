spring.application.name=sp500-10k-retrieval-demo

server.port=8080

# Vector and hybrid search require Redis OM Spring AI.
# The checked-in embedder example for this demo lives on FilingChunk.chunkText:
# @Vectorize(provider = TRANSFORMERS)
redis.om.spring.ai.enabled=true
